Optional reference library (curated clause-risk knowledge base and jurisdiction
notes) for the RAG-grounded analyzer described in SPEC.md §4 and Phase 3.

Empty in Phase 1 — the analyzer runs against the taxonomy in
`legal_agent.domain.clause_taxonomy` without external grounding.
