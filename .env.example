# Copy to .env and fill in. NEVER commit real keys (.env is git-ignored).

# Required only for model-backed analysis (`--use-model`).
ANTHROPIC_API_KEY=

# Optional: override the model used for clause analysis.
# LEGAL_AGENT_MODEL=claude-opus-4-8
